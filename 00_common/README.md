Shared configuration

Copy config.example.sh to config.sh and replace local placeholders. 
The real configuration file is ignored because it may contain server-specific paths. 
Confirm the dissertation-specific GQ and DP thresholds before running the filtering scripts.